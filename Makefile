install: 
	mise install

dev-backend:
	cd backend && npm run dev

dev-frontend:
	@echo "Ainda não há frontend para subir"

build:
	@echo "Ainda não há build para ser feito"

test:
	@echo "Ainda não há testes para serem feitos"

